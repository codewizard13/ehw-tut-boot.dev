<!-- 🔗 Custom Stylesheet -->
<link rel="stylesheet" href="../../_css/main.css">

<!-- 🖼️ Site Logo -->
![Site Logo](/_pix/logos/logo-ehw-kb-h32.png)


<!-- 📝 Title -->
# 📒 COURSE NOTES: <span class="course-title">[Learn Linux](https://www.boot.dev/lessons/4813b39b-ee97-4697-8486-f80b59abb194)</span>

## 📂 Chapter 01: **The Command Line**

* **Chapter URL:** https://www.boot.dev/lessons/2315c308-0123-4efc-8dba-839685403708


<!-- 🧭 Navigation -->
### [🏚️ README](../../README.md) | [📁 Index](index.md) | [🔖 Bookmark](#bookmark)

<span class="success-banner">DONE! (08/16/2026)</span>


<br>

**In this Chapter:**  

<section class="ehw-doc-descr">  

### Learn Linux, Ch. 01: *The Command Line*

These are my personal notes on **terminals, shells, command-line interfaces, environment variables, and command history** from Boot.dev. This chapter introduces the **command line** as an essential interface for interacting with Linux systems, teaching the difference between **CLI** and **GUI** paradigms, how shells interpret commands, and how to leverage **terminal history** for efficient workflows. Understanding the command line is foundational because it provides programmatic access to system resources and enables automation, scripting, and remote administration—critical skills for **server administration**, **software development**, and **DevOps automation**.

### Learning Outcomes  

By the end of this chapter, I should be able to:  

- Understand what a terminal, shell, and command line are, and how they differ from graphical interfaces
- Execute basic commands in a terminal and interpret terminal output
- Create, modify, and use environment variables in shell sessions
- Navigate command history efficiently using keyboard shortcuts and built-in history tools
- Recognize when to use CLI vs. GUI and choose the appropriate tool for different tasks
- Troubleshoot common terminal issues like command syntax errors and variable interpolation problems
- Explain why command-line proficiency is essential for system administration and software development

</section>

### Applied Examples

| #️⃣   | Example | Details |
| --- | ------- | ------- |
| 1   | Setting up environment variables | Configuring `$PATH` and `$HOME` variables used by system utilities and application deployment pipelines |
| 2   | Server administration workflows | Using command history and terminal shortcuts to efficiently manage remote systems and automate repetitive tasks |
| 3   | Development environment setup | Creating persistent shell variables for project paths, API keys, and build configurations in `.bashrc` or `.zshrc` |
| 4   | Debugging application logs | Using command-line tools to tail, grep, and filter logs from running services and containers |
| 5   | DevOps automation scripting | Leveraging shell knowledge to write deployment scripts and CI/CD pipeline commands |

<!-- 🏷️ RELATED TAGS -->
<section id="sec-tags">

## 🏷️ Tags:

- Linux
- Terminal
- Command Line (CLI)
- Shells
- Environment Variables
- System Administration

</section>

---


<!-- 📖 TOC (Table of Content) -->
<details open>

<summary>Table of Contents</summary>

- [📒 COURSE NOTES: Learn Linux](#-course-notes-learn-linux)
  - [📂 Chapter 01: **The Command Line**](#-chapter-01-the-command-line)
    - [🏚️ README | 📁 Index | 🔖 Bookmark](#️-readme---index---bookmark)
    - [Learn Linux, Ch. 01: *The Command Line*](#learn-linux-ch-01-the-command-line)
    - [Learning Outcomes](#learning-outcomes)
    - [Applied Examples](#applied-examples)
  - [🏷️ Tags:](#️-tags)
    - [▶️ 1: Welcome to Learn Linux](#️-1-welcome-to-learn-linux)
  - [Learning Goals](#learning-goals)
    - [▶️ 2: What Is a Terminal?](#️-2-what-is-a-terminal)
    - [▶️ 3: What Is a Shell?](#️-3-what-is-a-shell)
  - [REPL](#repl)
  - [Tip](#tip)
    - [▶️ 4: Command Line vs. GUI](#️-4-command-line-vs-gui)
  - [What's a CLI?](#whats-a-cli)
  - [What's a GUI?](#whats-a-gui)
    - [▶️ 5: Variables](#️-5-variables)
  - [Creating a Variable](#creating-a-variable)
  - [Using a Variable](#using-a-variable)
  - [Interpolating a Variable in a String](#interpolating-a-variable-in-a-string)
    - [▶️ 6: History](#️-6-history)
    - [▶️ 7: Navigate History](#️-7-navigate-history)
  - [Arrow Keys](#arrow-keys)
  - [Clear](#clear)


</details>
<!-- Lesson Notes -->


### ▶️ 1: Welcome to Learn Linux

<section class="callout"><span class="label-verbatim">COPIED VERBATIM:  👇🏽</span>

Welcome to "Learn Linux"! Admittedly, the course name is a _bit_ of a misnomer. It's called "Learn Linux" because "Linux" is what most students _think_ of regarding the concepts in this course. A more accurate title might be "Learn to use shells and terminals in a Unix-like environment," but that's a mouthful.

## Learning Goals

*   Understand the difference between a shell and a terminal
*   Learn how to navigate and use the file system using the command line
*   Learn how to install software using the command line
*   Understand how to manage your `PATH` and environment variables
*   Learn how to manage permissions and execute programs

This course will get you comfortable working on a command line, which is a skill that's going to be critical for the rest of your career as a programmer. So, instead of writing code in the browser, in this course you'll interact with a command line interface (CLI).

For the first part of the course, you don't even need to leave this page. We've embedded a real, working shell on the right. Later, once you're comfortable, you'll set everything up on your _own_ machine.

</section>

<section class="callout"><span class="label-verbatim">Assignment:  👇🏽</span>

The [`echo` command](https://en.wikipedia.org/wiki/Echo_(command)) prints text back out to you. It's the "hello world" of the command line.

The shell is where you run commands. Type a command, press Enter, and the shell runs it.

1.  [ ] In the shell on the right, type the following command:
    
    ```sh
    echo "I use Linux btw"
    ```
    
2.  [ ] Press Enter to run it.

**Submit** the shell checks.


</section>

<br>

<section class="assignment-answer-sec">

**📈 My Answer - OUTPUT RESULTS**

```sh
codewizard13:~$ echo "I use Linux btw"                                                                                                                             
I use Linux btw 
```

</section><!-- END .assignment-answer-sec -->


### ▶️ 2: What Is a Terminal?

<section class="callout"><span class="label-verbatim">COPIED VERBATIM:  👇🏽</span>

As we talked about, the terms "shell," "CLI," and "terminal" are often used to refer to the same thing: a program for issuing text-based commands.

But to get pedantic, the "terminal" is just one _specific part_ of that program. Historically, the word "terminal" meant a physical device that you could type commands into, essentially a keyboard and a screen.

![A physical terminal](/_pix/boot.dev__linux-101__ch01-L2__computer.jpg)

These days, when we say "terminal," we really mean "terminal _emulator_." A terminal emulator is a program that emulates a physical terminal. It's a program that lets you type commands into a window on your computer.

_Which_ commands you're able to use isn't determined by the _terminal emulator_, but by the _shell_, which we'll talk about later.

_Your terminal emulator is just responsible for drawing text on the screen and processing your keystrokes._

</section>



### ▶️ 3: What Is a Shell?

<section class="callout"><span class="label-verbatim">COPIED VERBATIM:  👇🏽</span>

So if your terminal is just a program that lets you issue text-based commands and renders the output of those commands...

...What is the program that _runs_ those commands???

That's a shell.

Shells do a lot of things, but their main job is to interpret the commands you type and execute them.

## REPL

Shells are often referred to as "REPLs." REPL stands for

*   Read
*   Eval (evaluate)
*   Print
*   Loop

This is a fancy way of saying that shells are programs that:

1.  Read the commands you type
2.  Evaluate those commands, usually by running _other_ programs on your computer
3.  Print the output of those commands
4.  Give you a new prompt to type another command and repeat


</section>

<section class="callout"><span class="label-verbatim">Assignment:  👇🏽</span>

The [`expr` command](https://man7.org/linux/man-pages/man1/expr.1.html) evaluates a simple expression and prints the result. It's a tiny program, but it's a great way to watch the shell "read, eval, and print."

1.  [ ] In the shell on the right, run:
    
    ```sh
    expr 123456 + 7890
    ```
    
2.  [ ] Then, using the _same syntax_, print the result of `420` **minus** `69` (using the `-` operator) to the shell.

**Submit** the shell checks.

## Tip

If you're confused about prompts vs outputs, here's a quick explanation. Here's a session with my shell:

```sh
wagslane:~$ echo hello
hello
wagslane:~$
```

This is the "prompt": `wagslane:~$`

It's just telling me that the shell is ready to accept a command.

This is the command I typed: `echo hello`

Once I pressed Enter, the shell read my command, evaluated it, and printed the output: `hello`

Then, the shell printed a new prompt so I can run a different command: `wagslane:~$`

</section>

<br>

<section class="assignment-answer-sec">

**📈 My Answer - OUTPUT RESULTS**

```sh
codewizard13:~$ expr 123456 + 7890                                                                                                                                 
131346                                                                                                                                                             
codewizard13:~$ expr 420 - 69                                                                                                                                      
351
```

</section><!-- END .assignment-answer-sec -->


### ▶️ 4: Command Line vs. GUI

<section class="callout"><span class="label-verbatim">COPIED VERBATIM:  👇🏽</span>

In other Boot.dev courses you may have run your code by clicking a "Run" button. But as a real™ developer, you'll likely run your code using a command line interface (CLI) instead. For example, in Python, you would run a Python code file like this:

```sh
python main.py
```

## What's a CLI?

You'll often hear the terms "terminal," "shell," "command line," "CLI," and "command prompt" used interchangeably (despite some technical differences) to refer to the same thing: a program that allows you to interact with your computer in a text-based way.

## What's a GUI?

If you don't have a technical background, you're probably used to interacting with your phone or computer using a [graphical user interface](https://en.wikipedia.org/wiki/Graphical_user_interface) (GUI). When you use a mouse to click on fancy icons, buttons, and menus, you're using a GUI.

Click to hide video

Your browser does not support playing HTML5 video. You can [download the file](https://storage.googleapis.com/qvault-webapp-dynamic-assets/lesson_videos/GUI-vs-CLI-1920x1080.mp4) instead. Here is a description of the content: GUI vs CLI

To be fair, it's usually easier to teach people to use a GUI than a CLI. You can simply point to things and say "click here" or "drag this there." But GUIs do have some drawbacks:

*   **They're weak.** You are given much more control over your computer through a CLI. With a GUI you're limited to the options that the developer of the GUI has given you.
*   **They're slow.** Once you know the commands to type, it's much faster to type them than to click through endless menus with a mouse.
*   **They're not as reproducible.** If you want to share a set of instructions, you can just copy and paste commands without worrying about screen sizes and user preferences.
*   **They're not automatable.** It's easy to write code that manipulates text (as you've seen in Python), but it's much harder to write code that manipulates GUIs.
*   **They're not as cool.** You will be invited to 90% fewer romantic outings if you are a GUI user.

</section>

<section class="callout"><span class="label-verbatim">Assignment:  👇🏽</span>

The `whoami` command is about as simple as commands get: it prints the username of the user you're currently logged in as.

**Answer the question.**

What does the <code>whoami</code> command do?

</section>

<br>

<section class="assignment-answer-sec">

**📈 My Answer - OUTPUT RESULTS**

```sh
Prints the username of the current user to the console
```

</section><!-- END .assignment-answer-sec -->


### ▶️ 5: Variables

<section class="callout"><span class="label-verbatim">COPIED VERBATIM:  👇🏽</span>

Once we get to using your local machine:

*   If you're using Ubuntu on WSL, you're probably running a [Bash](https://en.wikipedia.org/wiki/Bash_(Unix_shell)) shell.
*   If you're using macOS, you're probably running a [Zsh](https://en.wikipedia.org/wiki/Z_shell) shell.
*   If you're running full Linux, I pray you already know what you're using.

The point is that you're probably using Bash or Zsh, and for the purposes of this course, they're _basically_ the same. **Our in-browser shell is an in-memory Bash shell**, at least, as close to Bash as we can get in a browser.

Both Bash and Zsh are shells, and they also happen to be powerful programming languages. They have variables, functions, loops, and more. That said, only [crazy people](https://github.com/cgsdev0/bash-stack) write large programs in shell languages... shells are optimized for running other programs and writing small scripts, not for writing large applications.

## Creating a Variable

```sh
name="Lane"
```

Notice there are no spaces between the variable `name`, assignment operator `=` and value `"Lane"`.

## Using a Variable

Starting a line with the `$` symbol is a convention to show it's a shell prompt, and it should not be typed. The actual command is `echo $name`.

```sh
$ echo $name
Lane
```

Unlike in Python, where you can just use a variable's _name_, in your shell you need to prefix the variable name with a `$` to _use_ it, else it would just print `name` instead of the value.

## Interpolating a Variable in a String

```sh
$ echo Hello $name
Hello Lane
```

It's really just _that_ easy.

</section>

<section class="callout"><span class="label-verbatim">Assignment:  👇🏽</span>

For the rest of this course, you'll be a forensic developer hired by the fictional "WorldBanc" to investigate a security breach. Like most mega-corporations, they don't know what they're doing, but they're happy to overpay you so they can say a "third-party expert" reviewed their systems.

...Anyhow, you'll be using a shell to traverse files, manipulate text, run programs, and investigate suspicious activity.

1.  [ ] In the shell on the right, set the following three variables:
    *   `bankname` to `"WorldBanc"`
    *   `founded` to `"1969"`
    *   `ceo` to `"Jeff Gates"`
2.  [ ] Write and execute a command that prints the following by using [`echo`](https://en.wikipedia.org/wiki/Echo_(command)) and **the _dynamic_ variables you just set**:
    
    ```text
    WorldBanc was founded in 1969 by Jeff Gates
    ```
    

**Submit** the shell checks.


</section>

<br>

<section class="assignment-answer-sec">

**📈 My Answer - OUTPUT RESULTS**

```sh
codewizard13:~$ bankname="WorldBanc"                                                                                                                               
codewizard13:~$ founded="1969"                                                                                                                                     
codewizard13:~$ ceo="Jeff Gates"                                                                                                                                   
codewizard13:~$ echo $bankname was founded in $founded by $ceo                                                                                                     
WorldBanc was founded in 1969 by Jeff Gates   
```

</section><!-- END .assignment-answer-sec -->


### ▶️ 6: History

<section class="callout"><span class="label-verbatim">COPIED VERBATIM:  👇🏽</span>

When you're working in a REPL, it's _really_ helpful to be able to see the commands you've typed in the past. That way you can easily re-run them, or copy and paste them into a script. To print out the history of commands you've typed in your shell, you can use the [`history` command](https://www.gnu.org/software/bash/manual/html_node/Bash-History-Builtins.html).

```sh
history
```

</section>

<section class="callout"><span class="label-verbatim">Assignment:  👇🏽</span>

1.  [ ] In the shell on the right, set 2 variables:
    *   `department` to `engineering`
    *   `team` to `ops`
2.  [ ] Use `echo` to print the following message (using the `$department` and `$team` variables):
    *   `I work in engineering on ops`
3.  [ ] Run the `history` command to see the commands you've run so far.

**Submit** the shell checks.

Make sure you've copied all the above variables with **correct** spelling and capitalization... unless you like losing armor.

</section>

<br>

<section class="assignment-answer-sec">

**📈 My Answer - OUTPUT RESULTS**

```sh

codewizard13:~$ department="engineering"                                                                                                                           
codewizard13:~$ team="ops"                                                                                                                                         
codewizard13:~$ echo I work in $department on $team                                                                                                                
I work in engineering on ops                                                                                                                                       
codewizard13:~$ history                                                                                                                                            
1  department="engineering"                                                                                                                                        
2  team="ops"                                                                                                                                                      
3  echo I work in $department on $team                                                                                                                             
4  history  
```

</section><!-- END .assignment-answer-sec -->


### ▶️ 7: Navigate History


<section class="callout"><span class="label-verbatim">COPIED VERBATIM:  👇🏽</span>

You'll often want to re-run a command. You could just type it out again, but assuming you don't have the [WPM](https://en.wikipedia.org/wiki/Words_per_minute) of [ThePrimeagen](https://www.boot.dev/teachers/the-primeagen), that's a pain.

## Arrow Keys

You can use the ↑ and ↓ arrows to quickly cycle through your command history.

Pressing the ↑ key walks _backward_ through the commands you've run, one at a time. Once you've gone too far back, the ↓ key walks you forward again toward your most recent command. It's a huge time-saver: instead of retyping a long command, you just arrow up to it and press Enter.

## Clear

If your terminal is feeling cluttered with text, you can clear it with the `clear` command, or by pressing Ctrl+L.

This won't _delete_ your history, it will just clear the screen.

</section>



</section><!-- END .assignment-answer-sec -->


<!-- END Lesson Notes -->

---

