<!-- 🔗 Custom Stylesheet -->
<link rel="stylesheet" href="../../_css/main.css">

<!-- 🖼️ Site Logo -->
![Site Logo](/_pix/logos/logo-ehw-kb-h32.png)


<!-- 📝 Title -->
# 📒 COURSE NOTES: <span class="course-title">[Learn to Code in Python](https://www.boot.dev/lessons/78b4646f-85aa-42c7-ba46-faec2f0902a9)</span>


## 📂 Chapter 03: **Functions**

* **Chapter URL:** https://www.boot.dev/lessons/b4f5a4ef-9fb0-40f7-9dc6-baf28fe1be36


<!-- 🧭 Navigation -->
### [🏚️ README](../../README.md) | [📁 Index](index.md) | [🔖 Bookmark](#bookmark)


<span class="success-banner">DONE! (10/06/25)</span>


<br>

**In this Chapter:**


<section class="ehw-doc-descr">


These are my personal notes on **functions in Python** from Boot.dev. This chapter covers defining and calling `functions`, using multiple parameters, `default` values, `None` returns, and the difference between **printing** and **returning**. It also touches on function order, scope, and returning multiple values — all key to writing clean, modular code.


</section>


<!-- 🏷️ RELATED TAGS -->
<section id="sec-tags">

## 🏷️ Tags:

- Python
- Functions
- Parameters
- Modular Code
- Scope

</section>

---


<!-- 📖 TOC (Table of Content) -->
<details open>

<summary>Table of Contents</summary>

- [📒 COURSE NOTES: Learn to Code in Python](#-course-notes-learn-to-code-in-python)
  - [📂 Chapter 03: **Functions**](#-chapter-03-functions)
    - [🏚️ README | 📁 Index | 🔖 Bookmark](#️-readme---index---bookmark)
  - [🏷️ Tags:](#️-tags)
    - [▶️ L1: Functions](#️-l1-functions)
    - [▶️ L2: Function Review](#️-l2-function-review)
    - [▶️ L3: Function Review](#️-l3-function-review)
    - [▶️ L4: Multiple Parameters](#️-l4-multiple-parameters)
    - [▶️ L5: Printing vs. Returning](#️-l5-printing-vs-returning)
    - [▶️ L6: Where to Declare Functions](#️-l6-where-to-declare-functions)
    - [▶️ L7: Order of Functions](#️-l7-order-of-functions)
    - [▶️ L8: Order of Functions](#️-l8-order-of-functions)
    - [▶️ L9: Community](#️-l9-community)
    - [▶️ L10: Boots Interview](#️-l10-boots-interview)
    - [▶️ L11: Solutions](#️-l11-solutions)
    - [▶️ L12: None Return](#️-l12-none-return)
    - [▶️ L13: None Return](#️-l13-none-return)
    - [▶️ L14: Multiple Return Values](#️-l14-multiple-return-values)
      - [Receiving Multiple Values](#receiving-multiple-values)
    - [✅ **In JavaScript**:](#-in-javascript)
    - [✅ **In Perl**:](#-in-perl)
    - [Summary Table](#summary-table)
    - [▶️ L15: Parameters vs. Arguments](#️-l15-parameters-vs-arguments)
    - [▶️ L16: Parameters vs. Arguments](#️-l16-parameters-vs-arguments)
    - [▶️ L17: Default Values](#️-l17-default-values)
    - [How to create unit tests](#how-to-create-unit-tests)
    - [▶️ L18: Archmage](#️-l18-archmage)
    - [▶️ L19: Curse](#️-l19-curse)
    - [▶️ L20: Enchant and Attack](#️-l20-enchant-and-attack)
  - [References](#references)


</details>
<!-- Lesson Notes -->


### ▶️ L1: Functions


- **functions**: allow us to reuse and organize code; helps organize your code into smaller, modular pieces (**modular code**)
- **argument**: aka: input, parameter
- **def**: keyword for defining functions in Python

Ex:

```py
def area_of_circle(r):
    pi = 3.14
    result = pi * r * r
    return result

# Function is reusable:
area = area_of_circle(6)
print(area)
# 113.04

area = area_of_circle(7)
print(area)
# 153.86
```




### ▶️ L2: Function Review


<section class="callout"><span class="label-verbatim">Copied verbatim from the course 👇🏽</span>

Let's break down this function line by line so you can understand every nook and cranny of it.

```py
def area_of_circle(r):
    pi = 3.14
    result = pi * r * r
    return result

radius = 5
area = area_of_circle(radius)
print(area)
# 78.5
```

Here's a chronological explanation of what happens when the above code is executed:

1. `def area_of_circle(r)`

The `area_of_circle` function is defined for later use, but not called. It accepts a single input, the arbitrarily named `r`. The body of the function (`pi = 3.14.`.. etc) is ignored for now.

2. `radius = 5`
A new variable called `radius` is created and set to the value `5`.

3. `area_of_circle(radius)`
   
The `area_of_circle` function is called with `radius` (in this case 5) as the input. Finally, we jump back to the function definition.

4. `def area_of_circle(r):`

We will now start executing the body of the function, and `r` is set to `5`.

5. `pi = 3.14`

A new variable called `pi` is created with a value of `3.14`.

6. `result = pi * r * r`

Some simple math is evaluated (`3.14 * 5 * 5`) and stored in the `result` variable.

7. `return result`

The result variable is returned from the function as output.

8. `area = area_of_circle(radius)`

The returned value is stored in a new variable called `area` (in this case `78.5`).

9. `print(area)`

The value of `area` is printed to the console.

</section>

### ▶️ L3: Function Review

### ▶️ L4: Multiple Parameters



Functions can have multiple **parameters** (_inputs_)

By default, when you call a function with positional arguments, it's **the argument's position that determines which parameter receives it**. The first argument goes to the first parameter, the second to the second, and so on.

Ex:

```py
def create_introduction(name, age, height, weight):
    first_part = "Your name is " + name + " and you are " + age + " years old."
    second_part = "You are " + height + " meters tall and weigh " + weight + " kilograms."
    full_intro = first_part + " " + second_part
    return full_intro
```

It can be called like this:

```py
my_name = "John"
my_age = "30"

intro = create_introduction(my_name, my_age, "1.8", "80")
print(intro)
# Your name is John and you are 30 years old. You are 1.8 meters tall and weigh 80 kilograms.
```

### ▶️ L5: Printing vs. Returning

- **print()**: prints a value to console; does not return any values
- **return**: ends current function's execution and provides value(s) to the caller function; does not print anything to the console


<section class="info-banner"><span style="font-weight:700">💡 Printing to Debug Your Code</span>

Printing values and running your code is a great way to debug your code. You can see what values are stored in various variables, find your mistakes, and fix them. Add print statements and run your code as you go! It's a great habit to get into to make sure that each line you write is doing what you expect it to do.

</section>

<span class="boot-dev-op">Boot.dev Instructions:</span>

<section class="boot-dev-op-sec">

In the real world it's rare to leave `print()` statements in your code when you're done debugging. Similarly, you need to remember to remove any `print()` statements from your code before submitting your work here on Boot.dev because it will interfere with the tests!


</section>

### ▶️ L6: Where to Declare Functions

The following code doesn't work because the variable is called (`print()`) before it is defined,

```py
print(my_name)
my_name = 'Lane Wagner'
# NameError: 'my_name' is not defined
```

Similarly, the following function doesn't work because `main()` is being called **before it is defined** in the code. In Python, functions must be defined **before** they are called — there is no concept of **hoisting** as in JavaScript, where function declarations are automatically moved ("hoisted") to the top of their scope during interpretation.:

```py
main()  # ❌ NameError: name 'main' is not defined

def main():
    print("Hello")

```

The result is a runtime error:

<section class="error-banner">Error:

```sh
PythonError: Traceback (most recent call last):
  File "<exec>", line 16, in <module>
  File "<string>", line 1, in <module>
NameError: name 'main' is not defined
```

</section>

<br>

Therefore, to fix it we should move the `main()` call **after** the function definition:

```py
def main():
    print("Hello")

main()  # ✅ Works fine
```


<section class="info-banner"><span>💡 PRO TIP: </span>
In Python, functions must be defined <b><i>before</i></b> they are called
</section>


### ▶️ L7: Order of Functions


> 💡 To keep things organized, most developers **define all functions first**, then call `main()` at the end — ensuring Python has processed all definitions before execution begins.

> 💡 The entry point is typically called `main` by convention

Here's an example of a program with functions and entry point ordered properly:

```py
def main():
    health = 10
    armor = 5
    add_armor(health, armor)

def add_armor(h, a):
    new_health = h + a
    print_health(new_health)

def print_health(new_health):
    print(f"The player now has {new_health} health")

# call entrypoint last
main()
```


### ▶️ L8: Order of Functions



### ▶️ L9: Community

> 💡 **Getting help** is a **_critical part_** of learning to code (even in your day-to-day work as a developer)!

<span class="boot-dev-op">Discord:</span>

<section class="boot-dev-op-sec">

**Boots** and your **spellbook** are great first lines of defense when it comes to getting help. But, when you absolutely need a human instead, Boot.dev has an amazing [**Discord**](https://www.boot.dev/community) community full of helpful devs and fellow students eager to lend a hand.

- Click the "Community" tab below the lesson text (by Boots and the spellbook)
- Join the Boot.dev Discord server and sync your account
- With your account linked up, you'll be able to chat with other learners and mentors, as well as use the `#help...` forums to ask questions. Also, _**if you help others in the Discord, you'll get Karma which will help you earn [fellowship achievements](https://www.boot.dev/lessons/0f4fa755-1ce7-468b-bf34-c1460e97bf28#).**_
- Finally, check out the list of other recommended communities in the "Communities" tab. These are great places to hang out online... replace your junk-food social media with some great coding communities!

![Screenshot: Where to find the "Community" button in Boot.dev](/_pix/screens/screen-010.png)
</section>


<section class="info-banner"><span>💡 PRO TIP: </span>
Help others in the Discord to earn `fellowship achivement` awards.
</section>

**Formula to Calculate Temperature (Fahrenheit to Celsius)**:

```
5/9 * (f - 32)
```

Here's my answer to creating a `to_celsius()` function in Python:

```py
def to_celsius(f):
    c = (5/9) * (f - 32)
    return c

# Testing to validate function:

def test(f):
    c = round(to_celsius(f), 2)
    print(f, "degrees fahrenheit is", c, "degrees celsius")

test(100)
test(88)
test(104)
test(112)
```

### ▶️ L10: Boots Interview

<span class="boot-dev-op">Boots Interview:</span>

<section class="boot-dev-op-sec">

It's time for your first [Boots interview](https://www.boot.dev/lessons/dcafd50a-c53f-4a1a-8677-faf551ce4568#) lesson!

In these lessons, Boots turns on you, becoming your interviewer rather than just your tutor. He asks you an open-ended question, and you chat back and forth with him until he feels you've covered the points that the instructor has asked for.

- ⚠️ **If you ask follow up questions**, he'll clarify and **you will not be penalized**
- ⚠️ **If you give a partially correct answer**, you'll be asked to continue and **will not be penalized**
- ⛔ **If you say something that's wrong**, you'll **lose an armor/spree**
- ✅ Once you've covered all the points correctly, you'll pass

</section>


### ▶️ L11: Solutions

<span class="boot-dev-op">Instructor Solutions:</span>

<section class="boot-dev-op-sec">

Okay, I just need to tell you about one last resource you have when you're stuck. Every lesson on Boot.dev has an [**instructor solution**](https://www.boot.dev/lessons/6b3ce0b8-b323-4685-bf50-48cfd1c9959d#) that you can view!

- 💸 Like Boots, solutions come at a cost... a steeper one in fact. _**If you view a solution before you complete the lesson, you'll lose all the XP for that lesson.**_ Well, unless you use a **seer stone** - in which case you won't lose any XP. Though you will be down a seer stone.

- 🆓 Just like it's free to use Boots after a lesson is complete, **viewing a solution after you've completed the lesson is free** as well. In fact, we encourage you to frequently check your work against ours, often times there's something to learn from the differences.

- **View solutions sparingly**. If you find yourself using the solution before completion more than about once per chapter you should <u>_consider slowing down_</u>, <u>_asking for help in the discord_</u>, and even <u>_resetting lessons and trying them again_</u> to build a stronger foundation.

</section>


### ▶️ L12: None Return


- **none return**: - When no return value is specified in a function, it will automatically return `None`. For example, maybe it's a function that prints some text to the console, but doesn't explicitly return a value. 

The following code snippets all return the same value, `None`:

```py
def my_func():
    print("I do nothing")
    return None
```

```py
def my_func():
    print("I do nothing")
    return
```

```py
def my_func():
    print("I do nothing")
```


### ▶️ L13: None Return



### ▶️ L14: Multiple Return Values


- Python functions can return multiple values by separating them with a comma

**Ex:**

```py
def cast_iceblast(wizard_level, start_mana):
    damage = wizard_level * 2
    new_mana = start_mana - 10
    return damage, new_mana # return two values
```

#### Receiving Multiple Values

- when calling a function that returns multiple values, you can assign them to multiple variables, like this:

```py
dmg, mana = cast_iceblast(5, 100)
print(f"Damage: {dmg}, Remaining Mana: {mana}")
# Damage: 10, Remaining Mana: 90
```

- **order matters**

<section class="info-banner"><span>🧠 The More You Know: </span>

<br>

Our Python example uses **tuple unpacking** (also called **multiple assignment**) to assign multiple return values from a function to individual variables:

```python
dmg, mana = cast_iceblast(5, 100)
```

Here's what this concept is called or how it's handled in **JavaScript** and **Perl**:

---

### ✅ **In JavaScript**:

This is called **array destructuring** (or **object destructuring** depending on the return type).

**Example using array destructuring**:

```javascript
function castIceblast(power, mana) {
    let dmg = power * 2;
    let remainingMana = mana - 10;
    return [dmg, remainingMana];
}

let [dmg, mana] = castIceblast(5, 100);
console.log(`Damage: ${dmg}, Remaining Mana: ${mana}`);
// Output: Damage: 10, Remaining Mana: 90
```

> JS destructuring works for arrays and objects. Your function needs to return an array or object accordingly.

---

### ✅ **In Perl**:

This is called **list assignment**. Perl functions often return lists or arrays, and you can unpack them directly into variables.

**Example in Perl**:

```perl
sub cast_iceblast {
    my ($power, $mana) = @_;
    my $dmg = $power * 2;
    my $remaining_mana = $mana - 10;
    return ($dmg, $remaining_mana);
}

my ($dmg, $mana) = cast_iceblast(5, 100);
print "Damage: $dmg, Remaining Mana: $mana\n";
# Output: Damage: 10, Remaining Mana: 90
```

> In Perl, this is a very common idiom—returning and unpacking lists or arrays.

---

### Summary Table

| Language       | Term Used                | Example Keyword/Mechanism |
| -------------- | ------------------------ | ------------------------- |
| **Python**     | Tuple unpacking          | `a, b = func()`           |
| **JavaScript** | Destructuring assignment | `[a, b] = func()`         |
| **Perl**       | List assignment          | `my ($a, $b) = func()`    |

Let me know if you want object-based unpacking (e.g., in JavaScript), or if you're dealing with different return types.


</section>


### ▶️ L15: Parameters vs. Arguments

- **Parameters** are the variable names used in a function definition, while **arguments** are the actual values passed to the function when it's called. Although there's a technical difference, developers often use the terms interchangeably in practice.

**Ex:**

```py
# a and b are parameters
def add(a, b):
    return a + b

# 5 and 6 are arguments
sum = add(5, 6)
```


### ▶️ L16: Parameters vs. Arguments



### ▶️ L17: Default Values


In Python you can create **default values** by using the **assignement** (equals, `=`) operator in the function signature.

- **function signature**: the part of a function that defines its name, parameters, and sometimes return type—essentially how the function is called.

**Ex:**

```py
def get_greeting(email, name="there"):
    print("Hello", name, "welcome! You've registered your email:", email)

# Calling Ex. 1
get_greeting("lane@example.com", "Lane")
# Hello Lane welcome! You've registered your email: lane@example.com

# Calling Ex. 2
get_greeting("lane@example.com")
# Hello there welcome! You've registered your email: lane@example.com
```


<section class="info-banner"><span>💡 PRO TIP: </span>
Optional arguments (the ones with defaults) must come after all required arguments!
</section>


<section class="info-banner"><span>🧠 The More You Know: </span>

### How to create unit tests

Here's a quick example from the course demonstrating how tests can be set up.

```py
def get_punched(health, armor=1):
    damage = 50 - armor
    new_health = health - damage
    return new_health


def get_slashed(health, armor=0):
    damage = 100 - armor
    new_health = health - damage
    return new_health


# Don't touch below this line


def test(health, armor):
    print(f"Running tests for health {health} and armor {armor}")
    print("========================================")
    print(f"Health: {health}, Armor: {armor}")
    print(f"Health after punch: {get_punched(health, armor)}")
    print("----------------------------------------")
    print(f"Health: {health}, Armor: {armor}")
    print(f"Health after slash: {get_slashed(health, armor)}")
    print("----------------------------------------")
    print(f"Health: {health}, Armor: no armor!")
    print(f"Health after slash: {get_slashed(health)}")
    print("----------------------------------------")
    print(f"Health: {health}, Armor: no armor!")
    print(f"Health after punch: {get_punched(health)}")
    print("----------------------------------------\n")


test(400, 5)
test(300, 3)
test(200, 1)
```
</section>

**Output:**

```py
Running tests for health 400 and armor 5
========================================
Health: 400, Armor: 5
Health after punch: 355
----------------------------------------
Health: 400, Armor: 5
Health after slash: 305
----------------------------------------
Health: 400, Armor: no armor!
Health after slash: 300
----------------------------------------
Health: 400, Armor: no armor!
Health after punch: 351
----------------------------------------

Running tests for health 300 and armor 3
========================================
Health: 300, Armor: 3
Health after punch: 253
----------------------------------------
Health: 300, Armor: 3
Health after slash: 203
----------------------------------------
Health: 300, Armor: no armor!
Health after slash: 200
----------------------------------------
Health: 300, Armor: no armor!
Health after punch: 251
----------------------------------------

Running tests for health 200 and armor 1
========================================
Health: 200, Armor: 1
Health after punch: 151
----------------------------------------
Health: 200, Armor: 1
Health after slash: 101
----------------------------------------
Health: 200, Armor: no armor!
Health after slash: 100
----------------------------------------
Health: 200, Armor: no armor!
Health after punch: 151
----------------------------------------
```


### ▶️ L18: Archmage


<section class="warning-banner"><span style="font-weight:700; margin-right:1rem">💡 PRO TIP: </span>
This chapter and dictionaries are some of the hardest chapters in the course!
</section>


<section class="error-banner"><span style="font-weight:700; margin-right:1rem">GOTCHA: </span>

In Boot.dev, the user role remains "No Role" until Level 10 is reached; this is [expected behavior](https://discordapp.com/channels/551921866173054977/1424752960705007767), not a bug.

</section>

<span class="boot-dev-op">What to Know about your Role:</span>

<section class="boot-dev-op-sec">

As a reminder,

- 📦 Chests contain gems and items
- 💎 Gems can buy items in the [**shop**](https://www.boot.dev/lessons/4777c0b2-30fa-48fe-82bf-c9b84e74d92f#)
- 🏆 Items help you earn [**achievements**](https://www.boot.dev/lessons/4777c0b2-30fa-48fe-82bf-c9b84e74d92f#) and XP
- ⏫ XP levels you up, and higher levels 🔑 unlock new roles

So why should you care about your role?

My guess is that you've earned enough XP to be an `Apprentice` by now (🚨Aactually, role just says "No Role" _**until you hit level 10**_ ! )[^1][^2]. You can see your role at the top right of your screen. When you hit level `100`, you'll unlock the final role, `Archmage`. It's kind of a big deal. 

**When you hit ARCHMAGE:**

- You'll have learned a ton, built many **real-world projects**, and should be starting your job search
- You'll have unlocked all the **private role-based chat channels** in our [**Discord**](https://www.boot.dev/community)
- You'll have immeasurable bragging rights and a hoard of fake internet points
- You'll get **a physical archmage 🪙 coin in the 📫 mail** as a token of your hard work


![SCREENSHOT: Level 10 unlocks Role: Apprentice](/_pix/screens/screen-011.png)


</section>


### ▶️ L19: Curse

**The Purpose of Cursing**:
- an enemy's weapons can be cursed **so that they don't deal as much damage**


### ▶️ L20: Enchant and Attack

- In **Fantasy Quest**, a weapon can be **enchanted** to do **bonus damage**.



<!-- END Lesson Notes -->

---


## References

- N/A




[^1]: Eric's Discord Chat: https://discordapp.com/channels/551921866173054977/1424752960705007767
[^2]: TheShineyBears's Discord Chat: https://discordapp.com/channels/551921866173054977/1194009864192655391/1423136673021169754

